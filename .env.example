# Contact form endpoint. Any service that accepts a JSON POST works —
# Formspree, Resend, Basin, or your own API. Until this is set, the contact
# form tells the visitor it is not connected and offers the email fallback
# instead of faking a success state.
NEXT_PUBLIC_CONTACT_ENDPOINT=
